import styled from 'styled-components';
import { Tag } from 'antd';

export const StyledTag = styled(Tag)`
  && {
    border-radius: 8px;
    padding: 4px 10px;
    background: #181949;
    color: #fff;
    margin-right: 0;
  }
`;
